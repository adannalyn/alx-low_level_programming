#include "main.h"
/**
 * read_textfile - read from a file
 * @filename: name of file
 * @letters:
 * Return:
 */
ssize_t read_textfile(const char *filename, size_t letters)
{
	int fd;
	if (file == NULL)
	{
		return (0);
	}

	fd = open("filename", 0_RDWR | 0_CREAT);
	
}
