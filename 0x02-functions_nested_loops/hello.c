#include "main.h"
#include <stdio.h>
/**
 * _islower - checks for lowercase character
 * Return: 0
 */
int main(int alpha)
{
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		return (alpha);
	putchar('\n');
	return 0;
}
