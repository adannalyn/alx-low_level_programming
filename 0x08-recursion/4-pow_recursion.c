#include "main.h"
/**
 * _pow_recursion - print the power of a number
 * @x: first integer
 * @y: second integer
 * Return: 0
 */
int _pow_recursion(int x, int y)
{
	if (y < 0)
	{
		return (-1);
	}
}
