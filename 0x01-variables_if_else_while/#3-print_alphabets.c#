#include <stdio.h>
/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
	int count1, count2;

	for (count1 = 'A'; count1 <= 'Z'; count1++)
	{
		for (count2 = 'a'; count2 <= 'a'; count2++)
	       
			putchar(count2);
		}
	}
	putchar('\n');
	return (0);
}
