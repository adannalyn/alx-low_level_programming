#include "main.h"
#include <stdlib.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
void *malloc_checked(unsigned int b)
{
  int *c = 0;
   :     int *d = 0;
        c = calloc(b,sizeof (int));
        d = malloc(b: * sizof(int);
        if (c = NULL)
        {
                return (NULL);
        }
        else
        {
                return (98);
        }
		   }
