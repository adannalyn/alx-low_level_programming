#include "main.h"
/**
  * _strlen - length of a string
  * @s: a char
  * Return: 0
  */
int _strlen(char *s)
{
	int count;

	for (count = 0; count < s[count]; count++)
		count = count;
	return (count);
}
