#ifndef MAIN_H
#define MAIN_H

void print_string(char *str);
int main(int argc, char *argv[]);
int _atoi(char str[]);
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
int _putchar(char c);

#endif
