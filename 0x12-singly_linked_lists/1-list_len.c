#include "lists.h"
/**
 * list-len - return the number elements
 * @h: head pointer
 * Return: number of elements
 */
size_t list_len(const list_t *h)
{
	size_t count = 0;

	while (h != NULL)
	{
		count++;
		h = h->next;
	}
	return (count);
}
